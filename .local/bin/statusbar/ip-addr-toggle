#!/bin/bash

# Toggle /tmp/ip-addr-polybar-db
FILE="/tmp/ip-addr-polybar-db"
[ ! -f "$FILE" ] && echo "local" > $FILE && exit 0;


SELECTED=$(head -n 1 $FILE)
if [ $SELECTED = 'local' ];
then
	echo "public" > $FILE
else
	echo "local" > $FILE
fi

sh ~/.local/bin/statusbar/ip-addr
exit 0;
